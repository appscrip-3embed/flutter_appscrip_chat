import 'package:flutter/material.dart';

class IsmEditGroupTitleAndImage extends StatelessWidget {
  const IsmEditGroupTitleAndImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Placeholder();
}
